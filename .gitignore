# Dependencies
node_modules/
.pnpm-store/

# Git worktrees (isolated branch checkouts)
.worktrees/

# Build outputs (tsdown, tsc)
dist/
build/
*.tsbuildinfo
.tsbuildinfo

# Turborepo cache
.turbo/

# Vitest
coverage/
.vitest-cache/
*.lcov

# TypeScript incremental
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment / secrets
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
# keep examples committed
!.env.example
!.env.*.example

# Editor / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
.idea/
*.swp
*.swo
*~
.fleet/
.zed/

# OS
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Claude Code — local-only state (settings.json и rules/ коммитятся как shared config)
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/*.local.json
.claude/.cache/
.claude/state/
CLAUDE.local.md

# Superpowers brainstorming companion (mockups, server state)
.superpowers/

# Temp / scratch
tmp/
temp/
.tmp/
*.bak
*.orig

# Codegen output in examples (sample artifacts, не источник)
examples/*/generated/

# 1С test credentials / local fixtures (если кто-то положит локально)
# (snapshots/ коммитятся — это часть тестового baseline)
*.local.xml
*.local.json

# OS-specific archives
*.zip
*.tar.gz
*.tgz
!fixtures/*.tgz
