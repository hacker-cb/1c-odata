{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "rootDir": ".",
    "noEmit": true,
    "skipLibCheck": true,
    "paths": {
      "@1c-odata/mcp-server": ["./src/index.ts"]
    }
  },
  "// skipLibCheck": "See tsconfig.json — same third-party .d.ts (MCP SDK http transport, pino/thread-stream) plus msw/@mswjs interceptors don't satisfy the repo's strict skipLibCheck:false.",
  "// lib": "DOM here covers msw / @mswjs/interceptors .d.ts in tests; the production tsconfig.json also enables DOM (for @modelcontextprotocol/sdk's fetch-typed transport .d.ts). Both are server-only at runtime — no browser APIs are used.",
  "//": "Typechecks ALL test files (the main tsconfig.json excludes test/ for build). Self-package imports resolve to src via `paths`; cross-package imports resolve to their dist (built via turbo ^build).",
  "include": ["src/**/*", "test/**/*"]
}
