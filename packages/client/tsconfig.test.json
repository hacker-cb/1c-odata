{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "rootDir": ".",
    "noEmit": true,
    "paths": {
      "@1c-odata/client": ["./src/index.ts"],
      "@1c-odata/client/filter": ["./src/filter.ts"],
      "@1c-odata/client/internal": ["./src/internal.ts"]
    }
  },
  "//": "Typechecks ALL test files (the main tsconfig.json excludes test/ for build). Wired into `pnpm typecheck`. Self-package imports resolve to src via `paths` so the check never depends on a stale own dist; cross-package imports resolve to the dependency's dist (built via turbo ^build).",
  "include": ["src/**/*", "test/**/*"]
}
