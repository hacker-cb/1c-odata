{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "rootDir": ".",
    "noEmit": true,
    "paths": {
      "@1c-odata/metadata": ["./src/index.ts"]
    }
  },
  "//": "Typechecks ALL test files (the main tsconfig.json excludes test/ for build). Wired into `pnpm typecheck`. Self-package imports resolve to src via `paths`; cross-package imports (@1c-odata/client) resolve to its dist (built via turbo ^build).",
  "include": ["src/**/*", "test/**/*"]
}
